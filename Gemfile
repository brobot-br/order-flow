# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in order_flow.gemspec.
gemspec

group :development do
  gem 'rubocop', '~> 1.66', '>= 1.66.1'
end
